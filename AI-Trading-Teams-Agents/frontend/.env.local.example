# Base URL of the FastAPI backend that exposes the trading-agents pipeline
# over Server-Sent Events (see backend/app/main.py).
# Copy this file to .env.local and adjust if your backend runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:8000
